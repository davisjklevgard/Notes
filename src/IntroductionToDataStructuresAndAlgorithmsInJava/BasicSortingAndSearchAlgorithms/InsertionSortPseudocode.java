package IntroductionToDataStructuresAndAlgorithmsInJava.BasicSortingAndSearchAlgorithms;

public class InsertionSortPseudocode {
    /*
        for i = 0 to A.length-1
            current = A[i[
            j = i - 1
                while j >= 0 && A[j]>current
                A[j+1] = A[j]
                j = j - 1
        This is an example of a sorting algorithm that will probably never be used
     */
}
