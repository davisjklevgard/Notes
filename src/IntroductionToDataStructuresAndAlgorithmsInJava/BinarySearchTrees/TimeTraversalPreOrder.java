package IntroductionToDataStructuresAndAlgorithmsInJava.BinarySearchTrees;

public class TimeTraversalPreOrder {
    /*
        Pre Order
            1.Visit the root
            2. Traverse the left sub tree
            3. Traverse the right sub tree
        For large tree, the order will follow the root of each sub tree till you reach the end of the tree. The order
        will go through at the same as what is written

     */
    /*private void preOrder(TreeNode node) {
        if (node == null) {
            return;
        }
        System.out.printf("%s ", node.data);
        preOrder(node.left);
        preOrder(node.right);
    }*/
}
