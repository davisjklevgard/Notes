package JavaEssentialTrainingObjectsandAPIs.UseJavaPackagesAndLibraries;

public class WorkWithDataAndTimes {
    /*
        Old packages for time are under the java.util
        month indexing starts at 0
        the New package is under java.time
        month indexing starts at 1
        Use the old when working in an older java environment or with Android, for now.
        Use new for newer Java environments

        Creating Date of now
        Date example = new Date();

        to make a date from a past time:
        GregorianCalendar gcexample = new GregorianCalendar( 2009, 1, 28);
        Date d1 = gcexample.getTime();


        LocalDateTime ldt = LocalDateTime.now();
        prints a computer-readable time, so not ver helpful to humans

        formatting LocalDate
        DateTimeFormatter dtf = DateTimeFormatter.ofPattern("M/d'yyyy");
     */
}
