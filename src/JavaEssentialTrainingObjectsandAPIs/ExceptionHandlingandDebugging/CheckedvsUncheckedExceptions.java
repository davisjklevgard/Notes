package JavaEssentialTrainingObjectsandAPIs.ExceptionHandlingandDebugging;

public class CheckedvsUncheckedExceptions {
    /*Unchecked exception causes an application to exit, sometimes badly
        Any exception class that has runtime exception in its inheritance
        hierarchy is an unchecked exception

        Interrupted exception is a direct sub-class of exception, not runtime,
        and it must be declared

        throws (Exception) at the method header can allow for a program to run anyway

     */
}
