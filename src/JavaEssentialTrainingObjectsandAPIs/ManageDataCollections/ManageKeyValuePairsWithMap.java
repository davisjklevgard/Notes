package JavaEssentialTrainingObjectsandAPIs.ManageDataCollections;

import java.util.HashMap;
import java.util.Map;

public class ManageKeyValuePairsWithMap {
    Map<String, String> example = new HashMap<>();
    /*
      Hash map is a class used to represent unordered data, which contain key value pairs.

      EX:
      Map<String, String> example = new HashMap<>();
      map.put("Key1", "Value1");
      map.put("This is string1", "This is connected to string1");
      map.put("Since both are strings", "both need to be strings");

      Adding to the HashMap:

      map.put("This is how", "you add to a map");

      To get a value:
      String answer = map.get("Key1")
      printing out answer will give Value1 to the console

      Looking for a key that does not exist returns null

      Removing
      map.remove("This is how");

      to get a key:

     */
}
